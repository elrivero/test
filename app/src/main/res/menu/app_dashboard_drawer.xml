<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
            <item
                android:id="@+id/nav_home"
                android:icon="@drawable/ic_home_black_24dp"
                android:tint="@color/primary"
                android:title="Inicio" />
            <item
                android:id="@+id/nav_postparto"
                android:icon="@drawable/ic_forward_30_black_24dp"
                android:tint="@color/primary"
                android:title="Revisión Postparto" />
    </group>

    <item android:title="">
        <menu>
            <item
                android:id="@+id/nav_baston"
                android:icon="@drawable/ic_bluetooth_searching_black_24dp"
                android:tint="@color/primary"
                android:title="Baston" />
            <item
                android:id="@+id/nav_mangada"
                android:icon="@drawable/ic_lock_outline_white_36dp"
                android:tint="@color/primary"
                android:title="Limpiar mangadas" />
        </menu>
    </item>


    <item
        android:id="@+id/nav_predio"
        android:icon="@android:drawable/ic_menu_revert"
        android:tint="@color/primary"
        android:title="Cambiar Predio" />
    <item
        android:id="@+id/nav_logout"
        android:icon="@drawable/ic_power_settings_new_black_24dp"
        android:tint="@color/primary"
        android:title="Salir" />

</menu>
